# Frameworks and Nets

Building a framework from a net and building blocks, and reading the net back out of a crystal: the MOF builder, connection points, how symmetric nodes and linkers are turned, layer nets, and the RCSR.

:::{note}
This chapter is being written.  Until it is, the generated reference
in {ref}`the appendix <reference-appendix>` describes every command,
module and setting the application offers.
:::

## What this chapter will cover

- The MOF builder ({ref}`reference <mod-mof-build>`)
- Building blocks and connection points
- Layer nets and stacking
- Drawing a net over a crystal ({ref}`reference <mod-net-draw>`)
- Searching nets, transitivity and Systre
- The molecule builder ({ref}`reference <mod-build-molecule>`)
