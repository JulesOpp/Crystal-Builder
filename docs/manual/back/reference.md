(reference-appendix)=
# Reference

Written by the application from its own registries
(`.claude/skills/manual-writing/reference.py`), never by hand: every
command with its menu path, shortcut and description; every module
and each setting's type, range and default; every engine's options;
and every panel.

```{toctree}
:maxdepth: 2

../reference/commands
../reference/modules
../reference/engines
../reference/panels
```
